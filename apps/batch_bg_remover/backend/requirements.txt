rembg>=0.1.0
onnxruntime>=1.16.0
Pillow>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
requests>=2.31.0
httpx>=0.27.0
replicate>=0.34.0
huggingface-hub>=0.24.0

# ============================================================
# CLIPSeg 提示词分割引擎（可选，体积大）
# ============================================================
# torch + transformers 合计约 800MB 下载，首次使用 CLIPSeg 还会
# 自动下载模型约 1.5GB。不需要提示词分割功能可不装。
#
# 启动 run.py 时会检测并询问是否安装。
# 手动安装：
#   pip install transformers>=4.40.0 torch>=2.0.0
# GPU 版 PyTorch（NVIDIA 显卡）：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# ============================================================

# GPU 加速 rembg（可选）：
#   pip install rembg[gpu]
