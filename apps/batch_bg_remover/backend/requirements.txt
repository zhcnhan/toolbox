rembg>=0.1.0
onnxruntime>=1.16.0
# Apple Silicon (M1/M2/M3/M4) 建议安装 onnxruntime-silicon 替代 onnxruntime
# 可在 make-mac-app.sh 中自动完成，或手动执行：
#   pip uninstall onnxruntime -y && pip install onnxruntime-silicon
Pillow>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
requests>=2.31.0
httpx>=0.27.0
scipy>=1.9.0
numpy>=1.24.0

# ============================================================
# SAM 1 本地分割引擎（CPU 推理，不带 GPU 避免镜像过大）
# 模型文件需手动放置：~/.cache/sam/sam_vit_l_0b3195.pth
# 下载地址：https://dl.fbaipublicfiles.com/segment_anything/sam_vit_l_0b3195.pth
# ============================================================
segment-anything>=1.0

# GPU 加速 rembg（可选）：
#   pip install rembg[gpu]
