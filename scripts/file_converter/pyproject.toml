[project]
name = "file-converter"
version = "0.1.0"
description = "多格式文件互转命令行工具 — JSON / YAML / CSV / XML / TOML"
requires-python = ">=3.9"

[project.scripts]
file-converter = "file_converter.cli:main"

[project.optional-dependencies]
dev = ["pytest>=7.0"]
